#include <bits/stdc++.h>
#define ll long long 
#define int ll
using namespace std;



signed main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t = 1;
    //cin >> t; 
    while (t--)
    {
       
    }
    return 0;
}
